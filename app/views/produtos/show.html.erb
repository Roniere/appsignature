<p id="notice"><%= notice %></p>

<p>
  <b>Nomeproduto:</b>
  <%= raw(@produto.nomeproduto)%>
</p>

<p>
  <b>Descricao:</b>
  <%= raw(@produto.descricao) %>
</p>
<p>

	<%= image_tag @produto.foto1.url(:medium) %>
	<%= image_tag @produto.foto2.url(:medium) %>
	<%= image_tag @produto.foto3.url(:medium) %>



<%= link_to 'Edit', edit_produto_path(@produto) %> |
<%= link_to 'Back', produtos_path %>
